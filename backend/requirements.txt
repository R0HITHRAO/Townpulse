# ============================================================
# TownPulse Backend — Python Dependencies
# ============================================================

# FastAPI & ASGI
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database — SQLAlchemy + PostGIS
sqlalchemy==2.0.23
geoalchemy2==0.14.2
alembic==1.12.1
psycopg2-binary==2.9.9

# Pydantic (used by FastAPI)
pydantic==2.5.2
pydantic-settings==2.1.0
email-validator==2.1.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==41.0.7

# Redis
redis==5.0.1

# HTTP client (for external services)
httpx==0.25.2

# Celery (optional background tasks)
celery==5.3.4
kombu==5.3.3

# Twilio (optional, for OTP)
twilio==8.10.0

# SendGrid (optional, for email)
sendgrid==6.11.0

# AWS S3 (optional, for file storage)
boto3==1.33.6

# Monitoring & Logging
structlog==23.2.0
sentry-sdk[fastapi]==1.38.0
prometheus-fastapi-instrumentator==6.1.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.2
factory-boy==3.3.0

# Code quality
ruff==0.1.6
black==23.11.0
isort==5.12.0
mypy==1.7.1

# Utilities
python-dotenv==1.0.0
tenacity==8.2.3        # Retry logic for DB connections
faker==20.1.0          # Used in seed script
